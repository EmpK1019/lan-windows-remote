# LAN Remote 1.0.7

- 修复 Windows 已经解锁，但控制窗口仍停留在锁屏末帧的问题。
- 安全桌面 H.264 切源检测不再依赖下一帧到达；即使锁屏画面完全静止，也会独立检测会话状态并在解锁后退出安全源。
- 解锁后最迟约 100 毫秒关闭安全桌面流，随后启动新一代 Default 桌面 H.264 流。
- 修正锁屏场景下 Winlogon helper 的生命周期判定，同时保留 UAC 安全桌面的原有处理方式。
- 新增静止安全流解锁切源回归测试；完整 82 项 Python/HTTP 测试通过。
