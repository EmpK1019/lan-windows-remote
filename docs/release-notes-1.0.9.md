# LAN Remote 1.0.9

- 修复原生 D3D 视频在“适应窗口”模式下出现绿色上下留边的问题。
- BGRA 输出改用正确的 RGB 黑色背景，并在每帧合成前清理完整后缓冲，避免尺寸或显示模式切换后残留旧画面。
- 为视频处理器显式限定输出目标矩形，并同步加固“适应/填充”渲染状态。
- 新增真实 D3D 子窗口像素回归：30、60、120 帧模式下的上下留边 RGB 均验证为纯黑。
- 锁屏安全源切换、解锁后视频重建和打包版 WebView2/D3D 宿主回归通过。
